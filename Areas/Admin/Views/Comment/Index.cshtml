@model List<Comment>
@{
    ViewData["Title"] = "Index";
    int counter = 0;
}

<div class="pagetitle">
    <h1>Comments</h1>
    <nav>
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a asp-controller="dashboard" asp-action="index">Dashboard</a></li>
            <li class="breadcrumb-item">Comments</li>
        </ol>
    </nav>
</div><!-- End Page Title -->

<section class="section">
    <div class="row">

        <div class="col-lg-12">

            <div class="card">
                <div class="card-body">
                    <h5 class="card-title">Comments</h5>
                    </a>

                    @if (TempData["Message"] != null)
                    {
                        <div class="alert alert-success bg-success text-light border-0 alert-dismissible fade show" role="alert">
                            @TempData["Message"]
                            <button type="button" class="btn-close btn-close-white" data-bs-dismiss="alert" aria-label="Close"></button>
                        </div>
                    }
                    <!-- Table with stripped rows -->
                    <table class="table table-striped">
                        <thead>
                            <tr>
                                <th scope="col">#</th>
                                <th scope="col">Name</th>
                                <th scope="col">Email</th>
                                <th scope="col">Status</th>
                                <th scope="col">CreatedAt</th>
                                <th scope="col">Operations</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (Comment comment in Model as List<Comment>)
                            {
                                <tr>
                                    <th scope="row">@(++counter)</th>
                                    <td>@comment.Name</td>
                                    <td>@comment.Email</td>
                                    <td>
                                        @if(comment.Accepted == true)
                                        {
                                            <span class="badge bg-primary">Verified</span>
                                        }
                                        else
                                        {
                                            <span class="badge bg-danger">UnVerified</span>
                                        }
                                    </td>
                                    <td>@comment.CreatedAt.ToString("dd.MM.yyyy")</td>
                                    <td>
                                        <a class="btn btn-info" asp-controller="comment" asp-action="details" asp-route-id="@comment.Id">
                                            <i class="bx bx-edit"> </i>
                                        </a>
                                        @if (comment.Accepted == false)
                                        {
                                            <a class="btn btn-info" asp-controller="comment" asp-action="changestatus" asp-route-id="@comment.Id" asp-route-status="1">
                                                <i class="bx bx-check"> </i>
                                            </a>
                                        }
                                        else
                                        {
                                            <a class="btn btn-danger" asp-controller="comment" asp-action="changestatus" asp-route-id="@comment.Id" asp-route-status="0">
                                                <i class="bx bx-x"> </i>
                                            </a>
                                        }
                                        <a class="btn btn-danger delete" asp-controller="comment" asp-action="delete" asp-route-id="@comment.Id">
                                            <i class="bx bx-trash-alt"></i>
                                        </a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                    <!-- End Table with stripped rows -->

                </div>
            </div>

        </div>
    </div>
</section>
@section JS{
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script>
        let deletes = document.querySelectorAll('.delete');
        deletes.forEach(del => {
            del.addEventListener('click', function (e) {
                e.preventDefault();
                let url = del.getAttribute('href');
                Swal.fire({
                    title: 'Are you sure?',
                    text: "You won't be able to revert this!",
                    icon: 'warning',
                    showCancelButton: true,
                    confirmButtonColor: '#3085d6',
                    cancelButtonColor: '#d33',
                    confirmButtonText: 'Yes, delete it!'
                }).then((result) => {
                    if (result.isConfirmed) {
                        fetch(url)
                            .then(res => res.json())
                            .then(data => {
                                if (data.status) {
                                    Swal.fire(
                                        'Deleted!',
                                        data.message,
                                        'success'
                                    )
                                    setInterval(() => window.location.reload(), 1500);
                                } else {
                                    Swal.fire(
                                        'Deleted!',
                                        data.message,
                                        'error'
                                    )
                                    setInterval(() => window.location.reload(), 1500);
                                }
                            })
                    }
                })
            });
        })
    </script>
}


